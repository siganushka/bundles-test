{% extends 'base.html.twig' %}

{% block body %}
  <h3>Bundles Test!</h3>

  <hr />

  {% if form is defined %}
  <div class="bg-body-secondary p-3">
    {{ form(form) }}
  </div>
  {% endif %}
{% endblock %}
