{% extends 'base.html.twig' %}

{% block body %}
  <h3>Bundles Test!</h3>
  <div {{ stimulus_controller('hello') }}></div>

  <hr />

  {% if form is defined %}
  <div class="bg-body-secondary p-3">
    {{ form(form) }}
  </div>
  {% endif %}
{% endblock %}
