<div class="d-flex flex-column justify-content-center align-items-center text-body-tertiary w-100 h-100 py-3">
  <i class="bi bi-{{ icon|default('box2') }} fs-2"></i>
  <p class="m-0">{{ text|default('generic.no_results')|trans }}</p>
</div>
