App\Entity\User:
    attributes:
        id:
            groups: ['user:item', 'user:collection']
        identifier:
            groups: ['user:item', 'user:collection']
        password:
            groups: []
        lastLoginIp:
            groups: ['user:item', 'user:collection']
        lastLoginAt:
            groups: ['user:item', 'user:collection']
        roles:
            groups: ['user:item', 'user:collection']
        userIdentifier:
            groups: []
        enabled:
            groups: ['user:item', 'user:collection']
        createdAt:
            groups: ['user:item', 'user:collection']
        updatedAt:
            groups: ['user:item', 'user:collection']
