App\Entity\Topup:
    attributes:
        id:
            groups: [topup.item, topup.collection]
        title:
            groups: [topup.item, topup.collection]
        amount:
            groups: [topup.item, topup.collection]
        bonus:
            groups: [topup.item, topup.collection]
        payments:
            groups: [topup.item, topup.collection]
        createdAt:
            groups: [topup.item, topup.collection]
        updatedAt:
            groups: [topup.item, topup.collection]
